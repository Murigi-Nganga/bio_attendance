import 'package:flutter/material.dart';

class CourseUnitsTab extends StatelessWidget {
  const CourseUnitsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lecturer Course Units Tab'),
    );
  }
}
